version: "3"
services:
  nuxt-app:
    image: nuxt-ruksms
    stdin_open: true
    ports:
      - "3000:3000"
    networks:
      - ruksms
  api-server:
    image: express-ruksms
    ports:
      - "4000:4000"
    networks:
      - ruksms
    depends_on:
      - mongo
  mongo:
    image: mongo:3.6.19-xenial
    ports:
      - "27017:27017"
    networks:
      - ruksms
    volumes:
      - mongo-data:/data/db
networks:
  ruksms:
    driver: bridge
volumes:
  mongo-data:
    driver: local
